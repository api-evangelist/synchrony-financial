naftiko: 1.0.0-alpha2
info:
  label: Synchrony Financial Credit Authorization API — Reversals
  description: 'Synchrony Financial Credit Authorization API — Reversals. 1 operations. Lead operation: Create Reversal. Self-contained
    Naftiko capability covering one Synchrony Financial business surface.'
  tags:
  - Synchrony Financial
  - Reversals
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SYNCHRONY_FINANCIAL_API_KEY: SYNCHRONY_FINANCIAL_API_KEY
capability:
  consumes:
  - type: http
    namespace: credit-authorization-reversals
    baseUri: https://api.syf.com
    description: Synchrony Financial Credit Authorization API — Reversals business capability. Self-contained, no shared references.
    resources:
    - name: v1-authorizations-reversals
      path: /v1/authorizations/reversals
      operations:
      - name: createreversal
        method: POST
        description: Create Reversal
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.SYNCHRONY_FINANCIAL_API_KEY}}'
  exposes:
  - type: rest
    namespace: credit-authorization-reversals-rest
    port: 8080
    description: REST adapter for Synchrony Financial Credit Authorization API — Reversals. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/v1/authorizations/reversals
      name: v1-authorizations-reversals
      description: REST surface for v1-authorizations-reversals.
      operations:
      - method: POST
        name: createreversal
        description: Create Reversal
        call: credit-authorization-reversals.createreversal
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: credit-authorization-reversals-mcp
    port: 9090
    transport: http
    description: MCP adapter for Synchrony Financial Credit Authorization API — Reversals. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: create-reversal
      description: Create Reversal
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: credit-authorization-reversals.createreversal
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
